// StudySnap Extension Config — TEMPLATE
// 1. Copy this file to config.js (in the same folder)
// 2. Replace the placeholder values below with your actual keys
// 3. config.js is gitignored — never commit your real API key

const GEMINI_API_KEY = "YOUR_GEMINI_API_KEY_HERE";
const STUDYSNAP_URL = "http://localhost:3000";
const CHAT_MODE = false; // Set true → opens /chat instead of /processing
